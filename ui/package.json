{
  "name": "genvst-ui",
  "version": "0.3.1",
  "description": "Gen VST plugin UI — vanilla JS + Canvas, rendered in a JUCE 8 WebView",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
